steps:
# Step 1: Build the Docker image
- name: 'gcr.io/cloud-builders/docker'
  args:
    [
      'build',
      '-t',
      'us-central1-docker.pkg.dev/$PROJECT_ID/notes-app/gemini-notes-app:$COMMIT_SHA',
      '.'
    ]

# Step 2: Push the Docker image to Artifact Registry
- name: 'gcr.io/cloud-builders/docker'
  args:
    [
      'push',
      'us-central1-docker.pkg.dev/$PROJECT_ID/notes-app/gemini-notes-app:$COMMIT_SHA'
    ]

# Step 2.5: Substitute placeholders in Cloud Deploy and Skaffold configurations
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: 'bash'
  args:
    - '-c'
    - |
      set -x
      sed -i "s#GEMINI_PROJECT_ID_PLACEHOLDER#$PROJECT_ID#g" clouddeploy.yaml
      sed -i "s#GEMINI_PROJECT_ID_PLACEHOLDER#$PROJECT_ID#g" skaffold.yaml
      sed -i "s#GEMINI_PROJECT_NUMBER_PLACEHOLDER#$PROJECT_NUMBER#g" clouddeploy.yaml

# Step 3: Create a Cloud Deploy release
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: 'bash'
  args:
    - '-c'
    - |
      set -x
      printenv PROJECT_ID
      gcloud deploy releases create rel-${SHORT_SHA} \
        --project=${PROJECT_ID} \
        --delivery-pipeline=gemini-notes-app-pipeline \
        --region=us-central1 \
        --skaffold-file=skaffold.yaml \
        --images=gemini-notes-app=us-central1-docker.pkg.dev/${PROJECT_ID}/notes-app/gemini-notes-app:$COMMIT_SHA \
        --description="Cloud Build automated release"

images:
- 'us-central1-docker.pkg.dev/$PROJECT_ID/notes-app/gemini-notes-app:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY
